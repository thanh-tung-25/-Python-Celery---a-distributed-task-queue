# ==============================
# Flask + Celery Email Demo
# ==============================

# --- Redis Configuration ---
# Redis dùng để làm message broker & result backend
# Mặc định Redis chạy ở localhost port 6379
REDIS_URL=redis://localhost:6379/0


# --- SMTP Configuration ---
# Dùng Gmail hoặc SMTP server bất kỳ để gửi email thực tế
# Nếu để trống (""), chương trình sẽ giả lập việc gửi (không thật)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password

# --- Timezone ---
# Đặt múi giờ cho Celery và Flask
TIMEZONE=Asia/Ho_Chi_Minh
